<% include partials/header %>
<h2 class="page-header">Add Your Project(s)</h2>
<%if (typeof errors != 'undefined'){%>
<% if(errors){ %>
  
<% errors.forEach(function(error){ %>
  <div class="alert alert-danger"> <%=error.msg %> </div>  
<% }) %>

<%} %>
<%}%>
<body>
<form  enctype="multipart/form-data" method="POST" action="/users/addproject">
<div class="form-group">
    <label>Your first project link</label>
    <input type="text" class="form-control" placeholder="https://gist.github.com/aheckmann/2408370" name="link">
  </div>
  <h3> OR </h3>
  <div class="form-group">
    <label>Add a project as a screenshot</label>
 <input type="file" name="fileToUpload">
  </div>
  <button type="submit" class="btn btn-default">Add my project(s)</button>
 </form>
  </body> 
  <% include partials/footer %>